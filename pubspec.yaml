name: sensing_plugin
description: The sensing plugin for the Smart Sensing Library
version: 0.0.0
homepage:

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  pigeon: ^7.0.2

flutter:
  plugin:
    platforms:
      android:
        package: de.uni_ulm.sensing_plugin
        pluginClass: SensingPlugin
      ios:
        pluginClass: SensingPlugin
